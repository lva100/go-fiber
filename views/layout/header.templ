package layout

import "fmt"

css headerClass(percent int) {
	background: { "#222222" };
	text-align: { "center" };
	width: { fmt.Sprintf("%d%%",percent) };
}

templ Header() {
	<div class={headerClass(100)}>
		{children...}
	</div>
}