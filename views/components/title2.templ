package components

templ Title2(title string) {
	@Title2Style()
	<h2 class="title2">{title}</h2>
}

templ Title2Style() {
	<style>
		.title2 {
			text-align: center;
			font-size: 50px;
			font-weight: bold;
			margin-bottom: 40px;
			margin-top: 0;
			color: var(--color-black);
		}
	</style>
}